{
  "name": "acquisition-recon",
  "version": "0.1.0",
  "private": true,
  "description": "Outside-in digital acquisition audit for multi-location healthcare providers. Collects only publicly available data.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20.11"
  },
  "bin": {
    "recon": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "recon": "tsx src/cli.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx test/smoke.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.65.0",
    "cheerio": "^1.0.0",
    "commander": "^12.1.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
